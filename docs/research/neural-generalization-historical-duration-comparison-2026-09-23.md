# Historical source-duration comparison amendment

The original-corpus selection audit stopped because seven source-manifest durations were rounded to six decimal places while the historical score records retain the exact audiovisual-cache durations. The existing selections, labels, timestamps, valid masks, decoded intervals and metric durations are preserved. This amendment changes only the audit's comparison representation.

An independent audit of all eight original recordings verifies exact equality between the historical stored duration, both audiovisual-cache metadata durations and `frame_count / fps`. The source manifest must equal `round(cache_duration, 6)` exactly. Source groups, rally and ignored-interval values, tags, ordering, timestamps and valid masks retain their original contracts. No tolerance, endpoint clipping, new label revision or calibration outcome changes are permitted.

`analysis/neural_selection_duration_view.py` runs the unchanged selection-auditor body with its existing `evidence` dependency. A subclass overrides only `Evidence.document`: after verifying the original bytes, the exact historical source reference returns a derived comparison view. Only the seven declared `exactRows[i].durationSeconds` fields change to the independently verified cache values. Every other value, type and ordering in the source view is preserved, and every other document is returned unchanged. No auditor function is replaced. The body therefore runs against a derived comparison view, **not against unmodified source JSON**.

The proof preserves the original source identity, complete derived comparison document, canonical hashes, field-by-field changes and intercepted reference. The independent companion reconstructs the view and rechecks the cache timing, saved gold identity and unchanged selection. The final ordinary receipt keeps its `auditor` field solely as the delegated body identity for compatibility and prominently identifies the actual execution adapter and mandatory companion gate. Original-corpus receipts without the explicit execution declaration are rejected, including a stripped delegated base receipt.

The new `scripts/generalization-selection-audit-execution.py` workflow dispatches both this original-corpus comparison view and the previously registered endpoint-serialization adapter for randomized/proxy calibration. Both use the existing recall-containment correction gate unchanged. All 162 task selections and their ordinary, correction and applicable execution gates must freeze before any additional-video outcome metrics, including production comparisons, are opened. All 270 task/precision publication gates remain required by the final report audit. Raw label-blind inference may proceed beforehand under its separate gates.

The failed original audit log and successful saved selection remain as evidence. This amendment performs no training, model changes, recall-floor relaxation, metric normalization or source-label rewriting. Numerical selection uses the original historical cache durations throughout.
