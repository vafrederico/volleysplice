**Prospective baseline keep-head rescue experiment — 2026-09-19**

This protocol is fixed after the completed short-boost transfer study passed its full summary, independent tensor/scaler audit and independent interval audit, and before any real rescue candidate is evaluated. The reference report SHA-256 is `2f6e47590a8c7ee085dd8cebf0f626f833a6dfb7332216e284ade19839f1ec38`. The user authorized up to two further hypotheses. This is the second hypothesis: an existing auxiliary keep output may recover short rallies that the fixed live/serve/end decoder omitted.

The selected training cohorts are exactly `exact`, `draft`, and `reviewed_export`, in that order. Both architectures, `tcn` and `dino_tcn`, use seeds `3407`, `1729`, and `20260918`. Each of the eighteen baseline cells supplies an immutable reference and a rescue candidate, giving thirty-six result cells. There are no new fits, features, videos, annotations, model forwards, epochs or live/boundary decoder searches. The existing baseline loss arm is fixed even where a different loss arm performed better in the preceding study. The compact model remains the phone/browser candidate; frozen-DINO feature extraction remains a desktop route. This experiment measures decoder accuracy, not deployment latency.

All three cohorts are selected because their keep-head supervision differs: exact labels produce padded keep targets; the draft auxiliary tier does not supervise the keep head; reviewed exports add actual manually reviewed keep ranges. Their shared exact-label evaluation population, source groups and ignored intervals remain identical to the completed study. We will report every cohort and both architectures. No cohort is selected afterward by its result. Beach and the protected `source-group-008` source group remain excluded; no production promotion is permitted.

For each outer source group, retain the baseline's selected checkpoint epoch and live/serve/end decoder, including its smoothing. Select only one keep threshold from `[None, 0.35, 0.50, 0.65, 0.80]`, in that order. `None` is the exact no-op and wins exact score ties. Candidate selection uses only the three original inner validation sets, pooled across their recordings at the fixed baseline epoch. Eligible options have `R_core >= 0.95`; rank eligible options by descending `F1_padP_coreR`. If none is eligible, choose the highest F1 and explicitly flag infeasibility. Open each outer probability file only after selecting its threshold. The outer labels never tune this choice.

The proposal recipe is fixed by `analysis/neural_keep_rescue.py`. Read output head index 3, smooth independently within each valid tick segment using the unchanged baseline smoothing of 0.5 or 1 second, and threshold float32 probabilities with `>=` at the float32 threshold. Use actual nominal 4 Hz timestamps; do not retime frame-quantized caches. Component support extends from its first tick minus 0.125 seconds through its last tick plus 0.125 seconds, clipped to video bounds. Discard a component touching a valid-segment edge, touching a video edge, or intersecting/touching an exact ignored interval, including ignored holes between sample centers. Erode exactly two seconds at both ends and retain only strictly positive resulting core durations no longer than three seconds. Do not use live probabilities, boundaries, gold rally duration, feature values or labels as proposal gates.

Union the new cores with the unchanged baseline cores, merging only overlap or contact. Do not join positive gaps in this decoder step. No-op or proposals adding no effective coverage preserve the baseline core list exactly. Erosion is an approximate proposal heuristic: clipping, learned prediction error and gap joining make padded keep ranges non-invertible. Keep intervals are never directly scored as unpadded rally cores or padded twice.

Declare symmetric two-second product padding as the primary ranking case. For each fixed prediction set also report padding `(0,0)`, `(1,1)`, `(2,2)` and `(3,3)`: pooled `P_pad`, pooled `R_core`, `F1_padP_coreR`, padded model export seconds, padded human export seconds and their difference. Pad/clip/merge model and human ranges identically, then join positive gaps strictly shorter than three seconds; a gap of exactly three seconds remains a cut. Subtract ignored time from padded model, core human and padded human unions afterward, without rejoining across exclusions. Pool duration numerators/denominators across recordings within each seed before computing F1; report means across the three seed runs. Do not choose each model's best padding case.

Compare `selected-minus-reference` within each architecture and cohort. Retain both preceding development screens without relaxing their thresholds. The standard F1 screen requires a mean F1 gain of at least 0.02, positive gains in at least two seeds and a majority of source groups, and mean core-recall regression no worse than 0.005. The separate retention-recovery screen requires at least 20% fewer complete losses overall and among original rallies no longer than three seconds, reductions in at least two seeds for both counts, no increase in mean incomplete losses overall or in the short slice, mean F1/core-recall/long-rally-recall regression no worse than 0.005, and event-F1 regression no worse than 0.01. Incomplete means any complete or partial core loss after the declared product export processing. Report all inner feasibility flags and absolute outer recall as a diagnostic; the latter is not a new acceptance gate.

Standard-F1 replication and retention-recovery replication are separate: each requires its own screen and feasible inner selections in both architectures within the same cohort. Difference-in-differences is descriptive, with positive interaction not required. Baseline loss floors differ by architecture, so show the actual counts and avoid interpreting raw cross-architecture gains as transfer evidence. All conclusions remain adaptive development findings on previously inspected source groups.

The engineering preflight will bind the eighteen analysis sources and all reference inputs, run the prepared synthetic geometry/selection-isolation tests, and replay only the no-op against all eighteen baseline outer cells: seventy-two refit files and 144 recording decodes. It will evaluate no active rescue threshold. Register only after this passes. Keep the protocol, source copies, preflight plan/report/test log, registration, execution logs/receipts, completed report and independent summary immutable on the NAS. Do not read partial candidate quality to change this protocol. The independent summary must reconstruct candidate geometry, all inner threshold choices, held-out cuts, canonical metrics, guardrails and replication outcomes before interpretation.

Execution checklist:

- [ ] Publish the identical prospective protocol and source/input bindings on the NAS.
- [ ] Pass no-op and selection-isolation preflight.
- [ ] Register all thirty-six cells and freeze eighteen analysis sources.
- [ ] Execute the CPU-only rescue comparison and independent audit.
- [ ] Report every cohort, seed, padding case, guardrail and transfer result.
- [ ] Archive sources and final report after the preceding study archive is available.

The checklist is prospective and remains unchanged in the frozen protocol; completed status belongs in the separate results document and immutable receipts.
