# Explicit finalization and production feature-panel continuity

All scientific fitting, decoding, metric, calibration-floor and report-aggregation sources remain frozen. Every additional-video outcome remains closed until the complete162-selection freeze and independent cold file-identity audit pass. The long-lived evaluation worker calls the unchanged cache-v2 evaluation entry point with the exact CLI-equivalent arguments; it retains only module and identity metadata between cells and records per-cell memory and execution receipts.

Production raw inference was registered against the complete audiovisual feature index. Neural FP32 inference uses a later index that adds visual embeddings. A separate production panel binds the original audiovisual index and its passing42-record feature audit. An independent continuity gate requires the exact same manifest, source order, gold/ignored revision,34 scorable records, role policy, staging lineage and audiovisual cache references/bytes as the neural core panel. Neither the raw production protocol nor its probability archives are rewritten. The extra embeddings are outside production's input contract. Production evaluation calls the unchanged metric evaluator only after the same global selection and cold gates; its start and publication receipts bind this continuity audit.

The final numerical auditor has a separately named v2 source, preserving the original source and every numerical oracle function. Two historical provenance comparisons are corrected explicitly. The named metric audit and named control-replay audit have different schemas and must both pass; directory discovery must not interpret a control gate as a metric gate. Historical metric durations remain the exact registered audiovisual-cache/frame-count durations, while the inventory retains its existing six-decimal display values. The already-passing independent eight-record duration proof is rebound and replayed exactly, including timestamps, source groups, labels and valid masks. No interval, metric, tolerance, prediction, label or selected operating point changes.

`scripts/finalize-neural-generalization-v2.py` binds both numerical-auditor identities and this revision policy in a new finalization plan. It explicitly calls the unchanged containment preflight for all162 selections, execution preflight for all270 neural evaluations, global/cold-cache gate, and the new production publication gate before and after the declared v2 numerical audit. It does not replace any imported numerical function or alter a frozen wrapper. Its joint audit embeds only additional provenance around the exact delegated numerical audit; a strict restoration check protects every original field and the canonical `reportContentSha256` over inventory, scopes, series and task memberships. The unchanged report builder verifies this same digest before marking a report audited.

Sources and tests are frozen before use. The finalization plan binds all named sources, panel/audit identities and this protocol before metric finalization. Synthetic tests cover exact argument routing, partial failure preservation, audiovisual/source/gold/order mutations, required publication gates and unchanged numerical-payload restoration. These changes enable a coherent audit/publication path; they do not authorize an early external-data evaluation or a new fitting/selection experiment.
