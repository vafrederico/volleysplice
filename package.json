{
  "name": "volleycut",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "browser-assets": "node scripts/prepare-browser-assets.mjs",
    "predev": "npm run browser-assets",
    "dev": "next dev",
    "prebuild": "npm run browser-assets",
    "build": "next build",
    "start": "next start",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write .",
    "analysis:setup": "python3 -m venv .venv && .venv/bin/python -m pip install -r analysis/requirements.txt",
    "fixture:analysis": "bash scripts/create-analysis-fixture.sh",
    "dataset:status": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; python3 scripts/dataset-status.py",
    "analyze-no-model": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; .venv/bin/python -m analysis.heuristic_cli",
    "analyze-no-model:dataset": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; .venv/bin/python scripts/analyze-no-model-dataset.py",
    "reanalyze-no-model:dataset": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/reanalyze-no-model-dataset.py",
    "infer:model-dataset": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/infer-model-dataset.py",
    "infer:dual-serve-fusion-dataset": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/infer-dual-serve-fusion-dataset.py",
    "infer:specialist-model-dataset": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/infer-specialist-model-dataset.py",
    "import:exported-project-dataset": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/import-exported-project-dataset.py",
    "regenerate:exported-project-inference": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/regenerate-exported-project-inference.py",
    "regenerate:exported-project-eval-inference": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/regenerate-exported-project-eval-inference.py",
    "evaluate:model-padding": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-model-padding.py",
    "evaluate:model-features": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-model-features.py",
    "evaluate:transition-features": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-transition-features.py",
    "evaluate:court-relative-features": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-court-relative-features.py",
    "evaluate:multistate-features": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-multistate-features.py",
    "evaluate:multistate-followup": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-multistate-followup.py",
    "evaluate:component-selector": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-component-selector.py",
    "report:label-readiness": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/report-label-readiness.py",
    "report:transition-label-gate": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/report-transition-label-gate.py",
    "preflight:transition-pilot": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/preflight-transition-pilot.py",
    "extract:highres-embeddings": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-highres-embeddings.py",
    "extract:serving-side-v2": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-v2-features.py",
    "train:serving-side-v2": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/train-serving-side-v2.py",
    "extract:serving-side-v2-protected": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-v2-features.py --protected-test",
    "evaluate:serving-side-v2-protected": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-v2-protected.py",
    "evaluate:serving-side-v2-abstention": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-v2-abstention.py",
    "extract:serving-side-serve-gate": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-serve-gate.py",
    "extract:serving-side-flight": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-flight-features.py",
    "evaluate:serving-side-flight": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-flight.py",
    "evaluate:serving-side-flight-calibration": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-flight-calibration.py",
    "infer:serving-side-flight-all": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/infer-serving-side-flight-all-videos.py",
    "record:serving-side-uncertainty-review": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/record-serving-side-uncertainty-review.py",
    "build:serving-side-flight-controls": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/build-serving-side-flight-control-cohort.py",
    "evaluate:serving-side-flight-reviews": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-flight-review-slices.py",
    "extract:serving-side-temporal-flight": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-temporal-flight-features.py",
    "evaluate:serving-side-temporal-flight": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-temporal-flight.py",
    "extract:serving-side-trajectory": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-trajectory-features.py",
    "evaluate:serving-side-trajectory": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-trajectory.py",
    "extract:serving-side-selective-highres": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-selective-highres-features.py",
    "evaluate:serving-side-selective-highres": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-selective-highres.py",
    "extract:serving-side-selective-highres-ablation": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/extract-serving-side-selective-highres-features.py --configuration patch-12pct-96:0.12:96 --configuration patch-20pct-96:0.20:96 --configuration patch-30pct-96:0.30:96 --output /mnt/freenas/volleycut/labeling-v1-2026-08-09/features/serving-side-selective-highres-v2/development.json",
    "evaluate:serving-side-selective-highres-ablation": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-selective-highres-ablation.py",
    "evaluate:serving-side-quality-signals": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-quality-signals.py",
    "evaluate:serving-side-quality-mixture": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-quality-mixture.py",
    "evaluate:serving-side-group-balance": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-serving-side-group-balance.py",
    "infer:serving-side-v2-all-videos": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/infer-serving-side-v2-all-videos.py",
    "evaluate:highres-embeddings": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-highres-embeddings.py",
    "prepare:ball-pilot": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/prepare-ball-presence-pilot.py",
    "prepare:ball-sol-review": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/prepare-ball-presence-sol-review.py",
    "detect:ball-pilot": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/run-ball-presence-detector.py",
    "merge:ball-pilot": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/merge-ball-presence-review.py",
    "evaluate:ball-pilot": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-ball-presence-detector.py",
    "evaluate:labels": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; PYTHONPATH=. .venv/bin/python scripts/evaluate-labels.py",
    "test:analysis": ".venv/bin/python -m unittest discover -s analysis/tests -v",
    "test:web": "node --experimental-strip-types --test tests/web/*.test.mts",
    "validate:libswresample-wasm": "node scripts/validate-libswresample-wasm.mjs",
    "test": "npm run test:web && npm run test:analysis"
  },
  "dependencies": {
    "@techstark/opencv-js": "4.12.0-release.1",
    "fft.js": "4.0.4",
    "mediabunny": "1.53.1",
    "next": "16.3.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "assemblyscript": "^0.28.8",
    "typescript": "^7.0.2"
  }
}
